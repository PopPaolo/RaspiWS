/*
    This is the file that defines the highest level of the React app.
    @author Paolo Pop
 */
import "./App.css";
import Home from "./components/Home";

function App({theme}) {
  return (
    <Home/>
  );
}

export default App;

/*
    This is the file that defines the Background page and its components.
    @author Paolo Pop
 */

import {Typography} from "@mui/material";

function Background() {
    return (
        <Typography variant={"h1"}>
            This is the Background page
        </Typography>
    );
}

export default Background;

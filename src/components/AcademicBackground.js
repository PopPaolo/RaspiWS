/*
    This is the file that defines the Academic background page and its components.
    @author Paolo Pop
 */

import { Typography } from "@mui/material";

function AcademicBackground() {
    return (
      <Typography variant={"h1"}>
        This is the Academic Background page
      </Typography>

    );
}

export default AcademicBackground;
